# @software-factory/web

A small, deliberately boring Vite + React + TypeScript todo app used as a demo target for the software factory.

- `pnpm dev` — start the dev server
- `pnpm test` — run the test suite
- `pnpm lint` — run Biome checks
