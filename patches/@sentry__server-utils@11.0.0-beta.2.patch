diff --git a/build/cjs/ai/vercel-ai/index.js b/build/cjs/ai/vercel-ai/index.js
index 253f4518d4371617125cf6a3272bf1bd5e4dfdca..a30fa9e970c56eb89d6a1deb78be9aa6a1915d66 100644
--- a/build/cjs/ai/vercel-ai/index.js
+++ b/build/cjs/ai/vercel-ai/index.js
@@ -54,7 +54,12 @@ function getProviderMetadataAttributes(providerMetadata) {
   }
   return attributes$1;
 }
-const LAST_STEP_ONLY_USAGE_KEYS = /* @__PURE__ */ new Set([attributes.GEN_AI_USAGE_OUTPUT_TOKENS, attributes.GEN_AI_USAGE_TOTAL_TOKENS]);
+const LAST_STEP_ONLY_USAGE_KEYS = /* @__PURE__ */ new Set([
+  attributes.GEN_AI_USAGE_OUTPUT_TOKENS,
+  attributes.GEN_AI_USAGE_TOTAL_TOKENS,
+  attributes.GEN_AI_USAGE_CACHE_READ_INPUT_TOKENS,
+  attributes.GEN_AI_USAGE_CACHE_CREATION_INPUT_TOKENS
+]);
 function setAttributeIfDefined(attributes, key, value) {
   if (value != null) {
     attributes[key] = value;
diff --git a/build/cjs/integrations/vercel-ai/vercel-ai-dc-subscriber.js b/build/cjs/integrations/vercel-ai/vercel-ai-dc-subscriber.js
index b4af5b2f378f0392ddfc5f874c8f9bcb878616e8..b885422fd83345f5bfea49e639149e661ce0a606 100644
--- a/build/cjs/integrations/vercel-ai/vercel-ai-dc-subscriber.js
+++ b/build/cjs/integrations/vercel-ai/vercel-ai-dc-subscriber.js
@@ -164,6 +164,9 @@ function enrichInvokeAgentFromStream(callId, final, recordOutputs) {
     addTokensToSpan(span, attributes.GEN_AI_USAGE_INPUT_TOKENS, input);
     addTokensToSpan(span, attributes.GEN_AI_USAGE_OUTPUT_TOKENS, output);
     addTokensToSpan(span, attributes.GEN_AI_USAGE_TOTAL_TOKENS, tokenCount(usage.totalTokens) ?? util.sum(input, output));
+    const { cacheRead, cacheWrite } = cacheTokens(usage);
+    addTokensToSpan(span, attributes.GEN_AI_USAGE_CACHE_READ_INPUT_TOKENS, cacheRead);
+    addTokensToSpan(span, attributes.GEN_AI_USAGE_CACHE_CREATION_INPUT_TOKENS, cacheWrite);
   }
   if (recordOutputs) {
     const parts = partsFromTextAndToolCalls(final.text, final.toolCalls);
@@ -305,6 +308,7 @@ function enrichSpanOnEnd(span, data, channelOptions) {
     if (totalTokens !== void 0) {
       span.setAttribute(attributes.GEN_AI_USAGE_TOTAL_TOKENS, totalTokens);
     }
+    setCacheTokens(span, usage);
   }
   const finishReason = getFinishReason(result);
   if (finishReason && type === "languageModelCall") {
@@ -347,6 +351,23 @@ function getFinishReason(result) {
 function tokenCount(value) {
   return util.asNumber(value) ?? (core.isObjectLike(value) ? util.asNumber(value.total) : void 0);
 }
+function setCacheTokens(span, usage) {
+  const { cacheRead, cacheWrite } = cacheTokens(usage);
+  if (cacheRead !== void 0) {
+    span.setAttribute(attributes.GEN_AI_USAGE_CACHE_READ_INPUT_TOKENS, cacheRead);
+  }
+  if (cacheWrite !== void 0) {
+    span.setAttribute(attributes.GEN_AI_USAGE_CACHE_CREATION_INPUT_TOKENS, cacheWrite);
+  }
+}
+function cacheTokens(usage) {
+  const inputTokens = core.isObjectLike(usage.inputTokens) ? usage.inputTokens : void 0;
+  const inputTokenDetails = core.isObjectLike(usage.inputTokenDetails) ? usage.inputTokenDetails : void 0;
+  return {
+    cacheRead: util.asNumber(inputTokens?.cacheRead) ?? util.asNumber(inputTokenDetails?.cacheReadTokens) ?? util.asNumber(usage.cachedInputTokens),
+    cacheWrite: util.asNumber(inputTokens?.cacheWrite) ?? util.asNumber(inputTokenDetails?.cacheWriteTokens)
+  };
+}
 function buildOutputMessages(parts, finishReason) {
   if (!parts.length) {
     return void 0;
diff --git a/build/esm/ai/vercel-ai/index.js b/build/esm/ai/vercel-ai/index.js
index d4655ac21ed1e5db79be5d4a0b750a9c40f82ec4..e0563bae83c2decb9092b6109700d9194766406b 100644
--- a/build/esm/ai/vercel-ai/index.js
+++ b/build/esm/ai/vercel-ai/index.js
@@ -52,7 +52,12 @@ function getProviderMetadataAttributes(providerMetadata) {
   }
   return attributes;
 }
-const LAST_STEP_ONLY_USAGE_KEYS = /* @__PURE__ */ new Set([GEN_AI_USAGE_OUTPUT_TOKENS, GEN_AI_USAGE_TOTAL_TOKENS]);
+const LAST_STEP_ONLY_USAGE_KEYS = /* @__PURE__ */ new Set([
+  GEN_AI_USAGE_OUTPUT_TOKENS,
+  GEN_AI_USAGE_TOTAL_TOKENS,
+  GEN_AI_USAGE_CACHE_READ_INPUT_TOKENS,
+  GEN_AI_USAGE_CACHE_CREATION_INPUT_TOKENS
+]);
 function setAttributeIfDefined(attributes, key, value) {
   if (value != null) {
     attributes[key] = value;
diff --git a/build/esm/integrations/vercel-ai/vercel-ai-dc-subscriber.js b/build/esm/integrations/vercel-ai/vercel-ai-dc-subscriber.js
index ee5a880ddbe73846d440bf280a8f1c270196f10a..99794092b246b94841e7f63bb9c93eb7a36bf9d1 100644
--- a/build/esm/integrations/vercel-ai/vercel-ai-dc-subscriber.js
+++ b/build/esm/integrations/vercel-ai/vercel-ai-dc-subscriber.js
@@ -1,4 +1,4 @@
-import { GEN_AI_TOOL_CALL_RESULT, GEN_AI_USAGE_INPUT_TOKENS, GEN_AI_USAGE_OUTPUT_TOKENS, GEN_AI_USAGE_TOTAL_TOKENS, GEN_AI_RESPONSE_FINISH_REASONS, GEN_AI_RESPONSE_ID, GEN_AI_RESPONSE_MODEL, GEN_AI_CONVERSATION_ID, GEN_AI_OUTPUT_MESSAGES, GEN_AI_EMBEDDINGS_INPUT, GEN_AI_USAGE_REASONING_OUTPUT_TOKENS, GEN_AI_OPERATION_NAME, SENTRY_OP, GEN_AI_TOOL_CALL_ARGUMENTS, GEN_AI_TOOL_DESCRIPTION, GEN_AI_TOOL_NAME, GEN_AI_TOOL_DEFINITIONS, GEN_AI_FUNCTION_ID, GEN_AI_RESPONSE_STREAMING, GEN_AI_SYSTEM_INSTRUCTIONS, GEN_AI_INPUT_MESSAGES, GEN_AI_REQUEST_MODEL, GEN_AI_PROVIDER_NAME } from '@sentry/conventions/attributes';
+import { GEN_AI_USAGE_CACHE_READ_INPUT_TOKENS, GEN_AI_USAGE_CACHE_CREATION_INPUT_TOKENS, GEN_AI_TOOL_CALL_RESULT, GEN_AI_USAGE_INPUT_TOKENS, GEN_AI_USAGE_OUTPUT_TOKENS, GEN_AI_USAGE_TOTAL_TOKENS, GEN_AI_RESPONSE_FINISH_REASONS, GEN_AI_RESPONSE_ID, GEN_AI_RESPONSE_MODEL, GEN_AI_CONVERSATION_ID, GEN_AI_OUTPUT_MESSAGES, GEN_AI_EMBEDDINGS_INPUT, GEN_AI_USAGE_REASONING_OUTPUT_TOKENS, GEN_AI_OPERATION_NAME, SENTRY_OP, GEN_AI_TOOL_CALL_ARGUMENTS, GEN_AI_TOOL_DESCRIPTION, GEN_AI_TOOL_NAME, GEN_AI_TOOL_DEFINITIONS, GEN_AI_FUNCTION_ID, GEN_AI_RESPONSE_STREAMING, GEN_AI_SYSTEM_INSTRUCTIONS, GEN_AI_INPUT_MESSAGES, GEN_AI_REQUEST_MODEL, GEN_AI_PROVIDER_NAME } from '@sentry/conventions/attributes';
 import { GEN_AI_GENERATE_CONTENT, GEN_AI_EXECUTE_TOOL, GEN_AI_INVOKE_AGENT, GEN_AI_RERANK, GEN_AI_EMBEDDINGS } from '@sentry/conventions/op';
 import { isObjectLike, stringify, spanToJSON, _INTERNAL_skipAiProviderWrapping, getClient, SPAN_STATUS_ERROR, withScope, spanToTraceContext, captureException, startInactiveSpan, SEMANTIC_ATTRIBUTE_SENTRY_ORIGIN } from '@sentry/core';
 import { GEN_AI_TOOL_CALL_ID_ATTRIBUTE } from '../../ai/core/gen-ai-attributes.js';
@@ -162,6 +162,9 @@ function enrichInvokeAgentFromStream(callId, final, recordOutputs) {
     addTokensToSpan(span, GEN_AI_USAGE_INPUT_TOKENS, input);
     addTokensToSpan(span, GEN_AI_USAGE_OUTPUT_TOKENS, output);
     addTokensToSpan(span, GEN_AI_USAGE_TOTAL_TOKENS, tokenCount(usage.totalTokens) ?? sum(input, output));
+    const { cacheRead, cacheWrite } = cacheTokens(usage);
+    addTokensToSpan(span, GEN_AI_USAGE_CACHE_READ_INPUT_TOKENS, cacheRead);
+    addTokensToSpan(span, GEN_AI_USAGE_CACHE_CREATION_INPUT_TOKENS, cacheWrite);
   }
   if (recordOutputs) {
     const parts = partsFromTextAndToolCalls(final.text, final.toolCalls);
@@ -303,6 +306,7 @@ function enrichSpanOnEnd(span, data, channelOptions) {
     if (totalTokens !== void 0) {
       span.setAttribute(GEN_AI_USAGE_TOTAL_TOKENS, totalTokens);
     }
+    setCacheTokens(span, usage);
   }
   const finishReason = getFinishReason(result);
   if (finishReason && type === "languageModelCall") {
@@ -345,6 +349,23 @@ function getFinishReason(result) {
 function tokenCount(value) {
   return asNumber(value) ?? (isObjectLike(value) ? asNumber(value.total) : void 0);
 }
+function setCacheTokens(span, usage) {
+  const { cacheRead, cacheWrite } = cacheTokens(usage);
+  if (cacheRead !== void 0) {
+    span.setAttribute(GEN_AI_USAGE_CACHE_READ_INPUT_TOKENS, cacheRead);
+  }
+  if (cacheWrite !== void 0) {
+    span.setAttribute(GEN_AI_USAGE_CACHE_CREATION_INPUT_TOKENS, cacheWrite);
+  }
+}
+function cacheTokens(usage) {
+  const inputTokens = isObjectLike(usage.inputTokens) ? usage.inputTokens : void 0;
+  const inputTokenDetails = isObjectLike(usage.inputTokenDetails) ? usage.inputTokenDetails : void 0;
+  return {
+    cacheRead: asNumber(inputTokens?.cacheRead) ?? asNumber(inputTokenDetails?.cacheReadTokens) ?? asNumber(usage.cachedInputTokens),
+    cacheWrite: asNumber(inputTokens?.cacheWrite) ?? asNumber(inputTokenDetails?.cacheWriteTokens)
+  };
+}
 function buildOutputMessages(parts, finishReason) {
   if (!parts.length) {
     return void 0;
